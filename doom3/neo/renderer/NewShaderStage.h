#ifndef NEW_SHADER_STAGE_H
#define NEW_SHADER_STAGE_H

// Minimal stub so other TUs compile while real implementation is added.
class NewShaderStage {
public:
    NewShaderStage() = default;
    ~NewShaderStage() = default;
    // Expand with members only as other files require them.
};

#endif // NEW_SHADER_STAGE_H